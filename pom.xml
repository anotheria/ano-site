<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <!--
        Parent changed from ano-doc-parent to net.anotheria:parent directly.
        ano-doc-parent has been eliminated — its only remaining child was this project.
        The three things it provided (plugin execution, build-helper, clean) are
        declared explicitly in this pom's <build> section below.
    -->
    <parent>
        <groupId>net.anotheria</groupId>
        <artifactId>parent</artifactId>
        <version>4.2</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>ano-site</artifactId>
    <version>5.0.4</version>
    <name>anotheria.net site engine</name>

    <properties>
        <!--
            The one property to update before releasing ano-site: set this to the
            just-released ano-doc version, then run mvn release:prepare release:perform.
        -->
		<ano-doc.version>5.0.4</ano-doc.version>

        <ano-util.version>4.0.0</ano-util.version>
        <ano-tags.version>4.0.0</ano-tags.version>
        <ano-maf.version>4.0.0</ano-maf.version>
        <ano-prise.version>4.0.0</ano-prise.version>
        <ano-plass.version>4.0.0</ano-plass.version>
        <ano-access.version>4.0.0</ano-access.version>
        <jersey-version>3.1.3</jersey-version>
        <configureme.version>4.0.0</configureme.version>
        <moskito.version>4.0.3</moskito.version>
        <commons-io.version>2.15.0</commons-io.version>
        <json.version>20231013</json.version>
        <source-version>21</source-version>
        <target-version>21</target-version>
    </properties>

    <dependencies>
        <!--
            ano-doc is now a direct dependency — was previously inherited from ano-doc-parent.
            Version controlled by the ano-doc.version property above.
        -->
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-doc</artifactId>
            <version>${ano-doc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.servlet.jsp</groupId>
            <artifactId>jakarta.servlet.jsp-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.ws.rs</groupId>
            <artifactId>jakarta.ws.rs-api</artifactId>
            <version>3.1.0</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.core</groupId>
            <artifactId>jersey-client</artifactId>
            <version>${jersey-version}</version>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jersey.media</groupId>
            <artifactId>jersey-media-multipart</artifactId>
            <version>${jersey-version}</version>
        </dependency>
        <dependency>
            <groupId>org.codehaus.jettison</groupId>
            <artifactId>jettison</artifactId>
            <version>1.5.4</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-text</artifactId>
            <version>1.10.0</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-util</artifactId>
            <version>${ano-util.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-tags</artifactId>
            <version>${ano-tags.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-maf</artifactId>
            <version>${ano-maf.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-prise</artifactId>
            <version>${ano-prise.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-plass</artifactId>
            <version>${ano-plass.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>ano-access</artifactId>
            <version>${ano-access.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>moskito-core</artifactId>
            <version>${moskito.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>moskito-web</artifactId>
            <version>${moskito.version}</version>
        </dependency>
        <dependency>
            <groupId>net.anotheria</groupId>
            <artifactId>moskito-webui</artifactId>
            <version>${moskito.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons-io.version}</version>
        </dependency>
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>${json.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient</artifactId>
            <version>4.5.13</version>
        </dependency>
        <dependency>
            <groupId>com.theokanning.openai-gpt3-java</groupId>
            <artifactId>service</artifactId>
            <version>0.16.1</version>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>${project.basedir}/src/main/resources</directory>
            </resource>
            <resource>
                <directory>${project.basedir}/etc/def</directory>
                <includes>
                    <include>**/*as-*.xml</include>
                </includes>
                <targetPath>etc/def/as</targetPath>
            </resource>
            <resource>
                <directory>${project.basedir}/src/main/java</directory>
                <includes>
                    <include>**/*.jsp</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>net.anotheria</groupId>
                <artifactId>ano-doc-maven-plugin</artifactId>
                <version>${ano-doc.version}</version>
                <configuration>
                    <baseDir>${basedir}</baseDir>
                    <outputDir>${project.build.directory}/generated-sources/ano-doc</outputDir>
                </configuration>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--
                Register generated/java as an additional source root — moved here from ano-doc-parent.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>add-source</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-sources/ano-doc/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/gen/**</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${source-version}</release>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>3.6.3</version>
                    <configuration>
                        <additionalDependencies>
                            <additionalDependency>
                                <groupId>org.glassfish.jersey.core</groupId>
                                <artifactId>jersey-server</artifactId>
                                <version>3.1.3</version>
                            </additionalDependency>
                            <additionalDependency>
                                <groupId>org.glassfish.jersey.media</groupId>
                                <artifactId>jersey-media-multipart</artifactId>
                                <version>3.1.3</version>
                            </additionalDependency>
                        </additionalDependencies>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                            <configuration>
                                <additionalJOption>${javadoc.opts}</additionalJOption>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <scm>
        <url>https://github.com/anotheria/ano-site</url>
        <connection>scm:git:git@github.com:anotheria/ano-site.git</connection>
        <developerConnection>scm:git:git@github.com:anotheria/ano-site.git</developerConnection>
    </scm>

</project>
